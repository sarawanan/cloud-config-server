server:
  port: 9001

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sarawanan/config-server
          clone-on-start: true